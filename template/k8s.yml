apiVersion: apps/v1
kind: Deployment
metadata:
  name: instancing-fallback
  labels:
    app.kubernetes.io/part-of: ctfpilot
    app.kubernetes.io/name: instancing-fallback
    app.kubernetes.io/version: { .Version }
    app.kubernetes.io/component: challenges
    ctfpilot.com/component: instancing-fallback

spec:
  # HA setup
  replicas: 3
  selector:
    matchLabels:
      ctfpilot.com/component: instancing-fallback

  template:
    metadata:
      labels:
        ctfpilot.com/component: instancing-fallback
    spec:
      enableServiceLinks: false
      automountServiceAccountToken: false
      containers:
        - name: instancing-fallback
          image: ghcr.io/ctfpilot/instancing-fallback:{ .Version }
          imagePullPolicy: Always
          ports:
          - name:  http
            containerPort: 80
          resources:
            requests:
              memory: "128Mi"
              cpu: "10m"
            limits:
              memory: "256Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: instancing-fallback
  labels:
    app.kubernetes.io/part-of: ctfpilot
    app.kubernetes.io/name: instancing-fallback
    app.kubernetes.io/version: { .Version }
    app.kubernetes.io/component: challenges
    ctfpilot.com/component: instancing-fallback
spec:
  selector:
    ctfpilot.com/component: instancing-fallback
  ports:
  - name: http
    port: 80
    targetPort: http
